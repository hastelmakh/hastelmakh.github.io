{# @var project \App\Projects\Project #}

{% extends '@Projects~_shared/_project.html.twig' %}

{% block head %}
    {{ parent() }}
    {{ asset_import('@Projects~' ~ project.slug ~ '/main.js') }}
{% endblock %}

{% block content_project %}
    <section class="title">
        <div class="layout-content">
            <h1>{{ project.name }}</h1>
            <img src="{{ asset_path('@Projects~' ~ project.slug ~ '/images/byrd-logo.svg') }}" alt="byrd logo">
        </div>
    </section>

    <section class="cover">
        <div class="layout-content"></div>
    </section>

    <section class="about">
        <div class="layout-content">
            <div class="headline">
                <h2>About the Project</h2>

                <div class="description">
                    This project was created during an <b>internal Hackathon</b> at byrd, a SaaS platform where I worked
                    as a Product Designer. Our team aimed to design a self-service interface where customers could
                    manage their payment history without contacting support. The challenge: streamline a task that was
                    previously only possible via a help ticket.
                </div>
            </div>
        </div>
    </section>

    <section class="steps">
        <div class="layout-content">
            <div class="headline">
                <h2>Project Steps</h2>
            </div>

            <ol class="list">
                {% set items = [
                    {
                        'image': 'magnifier',
                        'name': 'Research',
                        'actions': ['Competitor Analysis', 'Stakeholder Interview'],
                    },
                    {
                        'image': 'brain',
                        'name': 'Ideation',
                        'actions': ['Brainstorming'],
                    },
                    {
                        'image': 'paintbrush',
                        'name': 'Design',
                        'actions': ['Wireframes', 'Hi-Fi Layouts', 'Prototype'],
                    },
                    {
                        'image': 'megaphone',
                        'name': 'Presentation',
                        'actions': ['Pitch Deck'],
                    },
                ] %}
                {% for item in items %}
                    <li>
                        <div class="card">
                            <div class="image {{ item.image }}" aria-hidden="true"></div>
                            <h3 class="name">{{ item.name }}</h3>
                        </div>
                        <ul class="actions">
                            {% for action in item.actions %}
                                <li>{{ action }}</li>
                            {% endfor %}
                        </ul>
                    </li>
                {% endfor %}
            </ol>
        </div>
    </section>

    <section class="problem">
        <div class="layout-content">
            <div class="headline">
                <h2>User Problem</h2>

                <div class="description">
                    Users couldn’t access invoices, check transactions, or track billing without support. This caused
                    frustration and unnecessary delays. For the Support Team, it meant a high volume of repetitive
                    requests.
                </div>
            </div>

            <div class="problem-table" role="table" aria-label="User problem details">
                {% set rows = [
                    {
                        'title': 'Who',
                        'subtitle': 'who is affected by the problem?',
                        'value1': 'Customers',
                        'value2': 'Customer Support Employees',
                    },
                    {
                        'title': 'What',
                        'subtitle': 'what is the problem?',
                        'value1': 'Don\'t have a centralized place where they can manage their invoices',
                        'value2': 'Overwhelmed with Customers requests (raised tickets) about providing invoices',
                    },
                    {
                        'title': 'Where',
                        'subtitle': 'where does this problem occur?',
                        'value1': 'Emails / Customer Dashboard',
                        'value2': 'Freshdesk',
                    },
                    {
                        'title': 'When',
                        'subtitle': 'when does the problem occur?',
                        'value1': 'At the end of each month',
                        'value2': 'At the end of each month',
                    },
                    {
                        'title': 'Why',
                        'subtitle': 'why does the problem occur?',
                        'value1': 'Users receive invoices only per email. So they have to manage them somehow',
                        'value2': 'No way to see if the users received invoice by email',
                    },
                ] %}
                {% for row in rows %}
                    <div class="row" role="row">
                        <div class="cell label" role="rowheader">
                            <div class="title">{{ row.title }}</div>
                            <div class="subtitle">{{ row.subtitle }}</div>
                        </div>
                        <div class="cell value" role="cell">{{ row.value1 }}</div>
                        <div class="cell value" role="cell">{{ row.value2 }}</div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </section>

    <section class="idea">
        <div class="layout-content">
            <div class="headline">
                <h2>Idea Process</h2>

                <div class="description">
                    We explored competitors, spoke with support staff, and gathered internal feedback. The insights
                    helped us imagine a simpler, clearer solution. Our goal was to anticipate user needs and make
                    answers instantly accessible.
                </div>
            </div>

            <ul class="list">
                {% set items = [
                    {
                        'image': 'download',
                        'hint': 'Download',
                        'title': 'Simplify invoice downloads',
                        'text': 'Ensure users can easily download invoices with one click.',
                    },
                    {
                        'image': 'filter',
                        'hint': 'Filtering',
                        'title': 'Add filtering options',
                        'text': 'Allow users to filter payment history by date, amount, or status + additional info.',
                    },
                    {
                        'image': 'file',
                        'hint': 'Status',
                        'title': 'Clear payment status labels',
                        'text': 'Use simple, recognisable labels like "Paid," "Pending," or "Overdue."',
                    },
                    {
                        'image': 'notifications',
                        'hint': 'Notifications',
                        'title': 'Automated reminders',
                        'text': 'Offer users the ability to set reminders for upcoming payments.',
                    },
                    {
                        'image': 'checklist',
                        'hint': 'Actions',
                        'title': 'Bulk invoice actions',
                        'text': 'Let users select and manage multiple invoices at once.',
                    },
                    {
                        'image': 'glasses',
                        'hint': 'Content',
                        'title': 'View details option',
                        'text': 'Let users click on an invoice for a detailed breakdown of charges.',
                    },
                ] %}
                {% for item in items %}
                    <li>
                        <div class="head">
                            <div class="image {{ item.image }}" aria-hidden="true"></div>
                            <div class="hint">{{ item.hint }}</div>
                        </div>
                        <h3>{{ item.title }}</h3>
                        <p class="text">{{ item.text }}</p>
                    </li>
                {% endfor %}
            </ul>
        </div>
    </section>

    <section class="focus">
        <div class="layout-content">
            <div class="headline">
                <h2>Focusing on What Matters Most</h2>

                <div class="description">
                    Together with the team, we identified key user tasks: view transactions, download invoices, filter
                    by date, track status, and access payment info. These tasks shaped our priorities. The result: a
                    lean, intuitive interface that covered real needs.
                </div>
            </div>

            <div>
                <img class="lightbox-on-small-vw" src="{{ asset_path('@Projects~' ~ project.slug ~ '/images/focus/chart.svg') }}" alt="byrd billing history functionalities chart">
            </div>
        </div>
    </section>

    <section class="figma">
        <div class="layout-content">
            <iframe src="https://embed.figma.com/design/toxF4kyFcJisvnj7pLLzIF/Portfolio---Billing-History?node-id=0-1&embed-host=hastelmakh&footer=false&theme=light" allowfullscreen></iframe>
        </div>
    </section>
{% endblock %}
